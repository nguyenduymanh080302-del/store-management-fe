type Product = {
    id: number
    units: Unit[]
    name: string
    slug: string
    images: string[]
    description: string
    categoryId: number
    category: Category
    createdAt: string
    updatedAt: string
}